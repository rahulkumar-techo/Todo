// styles.js

import { bgImg } from "../assets/imgs";

const styles = {
  body: {
    backgroundImage: `url(${bgImg})`,
    /* Additional styles for the background */
  },
};

document.add

export default styles;
